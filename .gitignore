bin/
obj/
.godot/

# Release zips built by package.sh.
dist/

# The game's own assembly - proprietary, not redistributable. Supply it yourself (see README.md).
# Includes version-suffixed copies kept for cross-version API comparison.
lib/sts2.dll
lib/sts2-*.dll

# Copied out of the game install beside sts2.dll. 0.110.0 made the game assembly reference Sentry, so the binding
# verifier cannot reflect over it without these present; nothing the mod itself compiles against. Not ours to ship.
lib/Sentry.dll
lib/Sentry.Godot.dll
